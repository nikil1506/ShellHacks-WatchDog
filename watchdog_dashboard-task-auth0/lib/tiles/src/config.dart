// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class colorConfig {
  // defining default indicator color as grey.shade100
  static final defaultIndicatorColor = Colors.grey.shade500;

  // defining default text color
  static const defaultTextColor = Colors.black;

  // defining default leadingicon color
  static const defaultIconColor = Colors.black;
}

class style {
// defining textstyle

  final defaultTextStyle =
      TextStyle(color: Colors.black, fontWeight: FontWeight.bold, fontSize: 20);
}
